https://github.com/lolinekos/succubus

yum update -y
yum install epel-release -y
yum groupinstall "Development Tools" -y
yum install gmp-devel -y
yum install screen wget bzip2 gcc nano gcc-c++ electric-fence sudo git libc6-dev httpd xinetd tftpd tftp-server mariadb-server gcc glibc-static -y

mysql_secure_installation

cd succubus

mysql -u root -proot -e "succubus;"

mysql -u root -proot succubus < doodump.sql

sh update.sh 

chmod 777 main  

./main 
